# 함께 고치기

이 저장소에는 규칙이 여덟 개 있습니다. 대부분 한 번씩 사고를 내고 나서 생긴 것입니다.
읽고 나서 고쳐 주시면 됩니다.

## 1. 고치기 전에 하던 작업을 커밋합니다

작업 폴더가 지저분한 채로 코드를 고치기 시작하면, 무엇이 이번 변경이고 무엇이 어제 것인지
갈라낼 수가 없습니다. `git status` 가 깨끗한 상태에서 시작해 주십시오.
AI 에게 수정을 맡길 때도 같습니다.

## 2. 새로 쓴 단언은 깨뜨려 봅니다

검사를 하나 추가했으면, **그 검사가 지키려는 코드를 일부러 망가뜨려 한 번 돌립니다.**
빨간 줄을 눈으로 본 다음에 되돌립니다. 이 과정을 거치지 않은 단언은 통과했다고 부르지 않습니다.

초록불만 보고 넘어간 검사는 아무것도 확인하지 않는 검사와 구분되지 않습니다.
값을 걸러 내는 규칙을 썼을 때도 같습니다. 거르려던 값을 직접 넣어 걸리는지 봅니다.

```bash
node server.js --test     # 함수 단위. 통과하면 가짓수를 세어 줍니다
python3 check-e2e.py      # 브라우저로 개설부터 성과까지
```

## 3. e2e 는 저장소 사본에서 돌립니다

`check-e2e.py` 는 `checklib.start()` 로 자기 서버를 띄웁니다. DB 와 포트는 임시로 잡지만
소스 파일은 작업 폴더에서 읽습니다. 검사가 도는 동안 파일을 고치면 무엇을 검사한 것인지
알 수 없게 됩니다. 폴더를 복사해 거기서 돌린 다음, 초록불을 본 뒤에 원본을 고칩니다.

## 4. `pkill -f` 를 쓰지 않습니다

`pkill -f node` 는 e2e 가 띄운 검사 서버까지 같이 죽입니다. 검사는 절반쯤 지나간 자리에서
멈추고, 이유는 로그에 남지 않습니다. 포트로 찾아 그것만 죽입니다.

```bash
lsof -ti:8788 | xargs kill
```

## 5. 열쇠를 코드에 넣지 않습니다

`KAKAO_KEY` `KAKAO_SECRET` `FEEDBACK_KEY` `APNS_KEY` `LOGO_TOKEN` `SEOUL_API_KEY` 는
코드에 적지 않습니다.
운영에서는 `fly secrets set` 으로 넣고, 코드에서는 이름만 부릅니다.
`.env` 파일은 커밋하지 않습니다. `.gitignore` 에 이미 들어 있습니다.

대회 열쇠(`okey` `jkey` `vkey` `tkey` `rkey` `pkey`)는 DB 가 만들고 DB 에만 있습니다.
소스나 검사 코드에 실제 대회의 열쇠를 적어 두지 않습니다.

## 6. 화면 문구는 한 줄 40자, `~합니다` 체

- 한 줄은 **40자 안**에서 끊습니다. 폰 폭에서 두 줄로 깨지면 단추를 밀어냅니다.
- 문장은 `~합니다` 체로 씁니다. 사용자가 읽는 글에 메모 말투가 섞이지 않게 합니다.
- **회색 설명은 카드당 한 줄**입니다. 두 줄이 되면 무엇이 중요한지 알 수 없게 됩니다.
  넣고 싶은 말이 두 줄이면 한 줄을 지웁니다.
- 값이 없을 때 0 이나 「없음」으로 그리지 않습니다. 상태는 있음·없음·모름 셋입니다.

## 7. 문서는 한 벌만 둡니다

같은 사실을 두 파일에 적지 않습니다. 운영 매뉴얼은 `GUIDE.md` 하나이고 `/manual` 이 그 파일을
읽어 화면으로 만듭니다. 개수나 목록을 문서에 손으로 적지 않습니다.
가짓수가 필요하면 `node server.js --test` 가 세어 줍니다.

## 8. 되돌리기 어려운 일은 먼저 말합니다

배포·삭제·덮어쓰기·초기화는 무엇을 지우고 무엇을 덮어쓰는지 먼저 적고 시작합니다.
운영 DB 를 `sqlite3` 로 직접 고치지 않습니다. 고칠 일이 있으면 API 를 거칩니다.

---

## 훅이 막는 것

`.claude/hooks/guard.py` 가 `.claude/settings.json` 의 `PreToolUse` 에 걸려 있습니다.
Bash 명령을 실행하기 전에 한 번 읽고, 아래에 걸리면 종료 코드 2 와 이유 한 줄로 막습니다.

| 막는 것 | 왜 |
|---|---|
| `pkill -f node` | e2e 가 띄운 검사 서버까지 죽습니다. 포트 PID 로 죽입니다 |
| `npx skills add` | 현재 폴더의 `.claude/skills` 에 설치됩니다. 저장소 안에서 돌리지 않습니다 |
| `git push --force` / `-f` | 원격 이력이 지워집니다 |
| `--remote-only` 없는 `fly deploy` | 이 환경에 로컬 도커가 없습니다. 그리고 검사 초록불 뒤에 배포합니다 |
| `rm -rf /` · `~` · `$HOME` | 홈과 루트를 지웁니다 |
| `.env` 를 `cat` · `open` · `echo` | 열쇠는 `fly secrets` 에만 둡니다 |
| 운영 DB 에 `DELETE` · `DROP` · `UPDATE` | 열린 대회가 그 자리에서 깨집니다 |

훅은 `Bash` 도구에만 걸립니다. 사람이 터미널에서 직접 치는 명령은 막지 않습니다.
규칙을 늘릴 때는 `guard.py` 의 `RULES` 한 곳만 고치고, 막으려던 명령을 넣어 실제로 막히는지 확인합니다.

## 코드에 손대지 않는 변경

문서만 고치는 변경이면 `server.js` `hack-on.html` `home.html` `check-e2e.py` 를 건드리지 않습니다.
문서 브랜치와 코드 브랜치를 나누면 리뷰가 훨씬 빨라집니다.
