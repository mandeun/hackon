# 보안

## 취약점을 찾으셨다면

**공개 이슈로 올리지 말아 주십시오.** 열린 대회가 있을 때 공개 이슈 하나가 그대로 공격 방법이 됩니다.

GitHub 의 **Security Advisories** 로 비공개 신고를 보내 주십시오.
저장소의 `Security` 탭에서 `Report a vulnerability` 를 누르면 비공개로 대화가 열립니다.
메일 주소는 따로 두지 않습니다.

같이 적어 주시면 확인이 빠릅니다.

- 어느 주소에서 무엇을 보냈을 때 생기는지
- 무엇이 새거나 무엇이 바뀌는지
- 가능하면 `curl` 한 줄이나 재현 순서

고쳐서 배포할 때까지 공개하지 말아 주시기를 부탁드립니다.
고친 뒤에는 신고해 주신 분 이름을 릴리스 노트에 적겠습니다. 원하지 않으시면 빼겠습니다.

## 열쇠 모델

이 서비스에는 **회원가입과 로그인이 없습니다.** 비밀번호도 메일 인증도 없습니다.
권한은 전부 열쇠로 나눕니다. 열쇠는 서버가 만든 무작위 문자열이고 DB 에만 있습니다.

| 열쇠 | 어디에 저장되나 | 어떻게 전달하나 | 무엇을 여나 |
|---|---|---|---|
| 운영 열쇠 | `events.okey` | `x-okey` 헤더 또는 주소의 `?k=` | 그 대회 하나의 운영 화면 |
| 주최자 열쇠 | `owners.id` | `x-owner` 헤더 또는 서명 쿠키 `hackon_s` | 그 사람이 연 대회 전부 |
| 심사 열쇠 | `events.jkey` | `x-jkey` 헤더. 링크는 `/j/<대회id>?k=` | 점수와 심사평 입력 |
| 투표 열쇠 | `events.vkey` | `x-vkey` 헤더. 링크는 `/v/<대회id>?k=` | 관객 평가가 켜진 대회의 표 |
| 팀 열쇠 | `teams.tkey` | `x-tkey` 헤더. 링크는 `/e/<대회id>?t=` | 제출·질문·설문·상호평가 |
| 받는 사람 열쇠 | `requests.rkey` | `x-rkey` 헤더. 링크는 `/r/<요청id>?k=` | 올린 주제의 상태와 판정 |
| 후원 열쇠 | `pledges.pkey` · `offers.pkey` | 주소의 `?k=`. 링크는 `/s/<ref>?k=` | 내가 맡은 자리의 확인 상태 |

운영 열쇠와 주최자 열쇠는 둘 중 하나만 맞으면 운영 권한이 열립니다.
대회 하나를 다른 사람에게 넘길 때 운영 열쇠만 주면 되도록 갈라 둔 것입니다.

틀린 주최자 열쇠를 여러 번 넣으면 그 IP 에 잠시 429 가 나갑니다.
맞는 열쇠는 세지 않습니다. 평소에 쓰는 사람이 먼저 막히지 않게 하려는 것입니다.

### 열쇠를 잃으면

| 잃은 열쇠 | 되찾는 길 |
|---|---|
| 주최자 열쇠 | 카카오 계정에 묶어 두었으면 다시 로그인하면 따라옵니다. 안 묶어 두었으면 길이 없습니다 |
| 운영 열쇠 | 주최자 열쇠가 있으면 그것으로 엽니다. 둘 다 없으면 길이 없습니다 |
| 심사 열쇠 · 투표 열쇠 | 운영자가 `/api/events/<대회id>/rekey` 로 새로 만듭니다. 옛 링크는 그 자리에서 죽습니다 |
| 팀 열쇠 | 운영자가 연락처로 본인을 확인하고 `/api/teams/<팀id>/relink` 로 링크를 다시 보냅니다 |
| 후원 열쇠 | 운영자가 `/api/give/<ref>/rekey` 로 새로 만들어 그 사람에게만 보냅니다 |
| 받는 사람 열쇠 | 새로 만들려면 지금 열쇠가 필요합니다. 잃으면 길이 없습니다 |

**되찾을 길이 없는 열쇠가 있습니다.** 화면이 열쇠를 처음 보여 줄 때 적어 두시기를 권합니다.
운영자가 마음대로 남의 열쇠를 다시 만들지 못하게 막아 둔 결과입니다.

심사·투표 링크가 대화방이나 공용 화면으로 흘렀다면 운영 화면의 「심사 열쇠 새로」와
「투표 열쇠 새로」를 누르십시오. 옛 링크가 그 자리에서 막힙니다.

## 열쇠 없이 되는 쓰기에는 상한이 있습니다

신청·후원·질문·피드백·문제 올리기는 열쇠 없이 됩니다. 처음 오는 사람에게 열쇠를 요구하면
아무도 안 들어옵니다. 대신 상한을 걸어 둡니다. 상한은 전부 환경변수로 조절합니다.

| 상한 | 무엇을 세나 | 넘으면 |
|---|---|---|
| `WRITE_LIMIT` | IP + 주소 단위로 열쇠 없는 쓰기 | 429 |
| `APPLY_LIMIT` | IP + **대회** 단위로 참가 신청 | 429 |
| 열쇠 대조 실패 | 틀린 주최자 열쇠를 넣은 IP | 429 |

`/mcp` 는 `/api/` 밖에 있는 주소입니다(클로드·AI 채팅이 그대로 부릅니다).
여기서 유일하게 쓰기를 하는 `post_problem` 도 같은 `WRITE_LIMIT` 을 지납니다. 읽기 도구는 세지 않습니다.

**남은 것:** 열쇠 없이 올라온 문제는 지금도 바로 공개 목록에 걸립니다.
운영자가 「대기 → 승인」으로 받는 줄은 아직 없습니다. 지금은 상한만으로 막고 있습니다.
승인 줄을 만들려면 대회 하나가 아니라 **사이트 전체**를 보는 권한이 먼저 있어야 하는데,
이 서비스에는 그런 자리가 없습니다(회원가입이 없습니다). 그 자리를 만드는 것이 먼저입니다.

## 마감 전에는 결과물을 내보내지 않습니다

제출 마감 전에는 공개 응답에서 제출 링크와 점수를 지웁니다.
먼저 낸 팀의 결과물을 뒤에 내는 팀이 보고 만드는 것을 막기 위해서입니다.
심사 화면도 같습니다. 운영자만 마감 전에 링크를 봅니다.
자세한 규칙은 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) 에 적었습니다.

## 개인정보

- **연락처는 그 대회의 운영자만 봅니다.** 서버가 공개 응답에서 연락처 칸을 지웁니다.
  화면에서만 감추면 브라우저 콘솔에 그대로 보입니다.
  공개 페이지·현장 큰 화면·결과 보고서에 연락처가 실리지 않습니다.
- 자리 맡기와 제안의 공개 장부에는 이름과 소속만 나갑니다.
- 협찬사에는 「협찬사 제공 동의」를 켠 참가자의 이메일만, 그 대회의 협찬사에만 갑니다.
- 개인정보 수집·이용 동의 없이는 서버가 참가 신청을 받지 않습니다. 동의한 시각을 남깁니다.
- 촬영과 사진 공개 동의는 따로 받습니다. 쇼케이스 노출도 본인이 켜야 생깁니다.
- **보관 기간은 대회 종료 후 6개월**입니다. 그 뒤 지웁니다.
  운영자가 대회를 지우면 그 자리에서 함께 지워집니다.
- 광고 추적, 제3자 분석 도구, 위치 수집, 연락처 접근을 하지 않습니다.

전문은 배포판의 [`/privacy`](https://hackon.mandeun.com/privacy) 에 있습니다.

## 이 저장소에 올리지 않는 것

실제 대회의 열쇠, `.env` 파일, 참가자 연락처가 든 내보내기 파일, 운영 DB 사본을 커밋하지 않습니다.
`.gitignore` 에 `*.db` 와 `data/` 가 들어 있습니다.
