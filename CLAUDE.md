# HACK:ON — 작업 규칙

Node 24 · 단일 `server.js` · 정적 HTML. 프레임워크·빌드 단계 없음.

## 먼저 할 것

여러 사람과 에이전트가 같은 `master` 를 동시에 고친다. 기능을 붙이기 전에:

```bash
git fetch origin
git log --oneline -15 origin/master
grep -n "<붙이려는 기능의 핵심 낱말>" server.js *.html
```

이미 있으면 새로 만들지 말고 그것을 고친다. 같은 기능을 두 갈래로 만들면 나중 것이 앞의 것을 가린다.

작업은 항상 `master` 가 아닌 **새 브랜치**에서 하고 PR 로 올린다.

## 검사

```bash
npm test               # 단위 — node server.js --test
python3 check-e2e.py   # e2e — playwright 필요
```

둘 다 초록이어야 PR 을 연다. 검사를 못 돌렸으면 PR 본문에 "검사 못 돌림"이라고 적는다.

**새 단언을 쓰면, 그 단언이 지키려는 코드를 일부러 망가뜨려 한 번 돌린다.** 빨간 줄을 본 다음 되돌린다. 이 과정을 안 거친 단언은 "통과했다"고 말하지 않는다.

기능 하나를 끝낼 때마다 커밋한다. 마지막에 몰아서 하지 않는다.

## 이 저장소에서 자주 밟는 것

- **새 정적 파일을 추가하면 `server.js` 의 `STATIC_OK` 화이트리스트에도 적는다.** 안 적으면 404 가 된다. HTML 이 참조하는 로컬 자산이 전부 200 인지 e2e 로 확인한다.
- 운영자 열쇠는 **헤더로만** 받는다(`x-pkey`). 주소창 `?k=` 로 받지 않는다.
- 대회 사본을 만들 때 `owner`·`jkey`·`vkey`·`tkey`·`pkey`·`rkey` 를 지운다. 되살릴 때는 새 열쇠를 발급한다.
- 값이 없을 때 상태는 셋이다 — **있음 / 없음 / 모름.** 집계 건수가 0 인 것과 아직 모르는 것을 같은 화면으로 그리지 않는다.
- 열쇠 없는 쓰기 길에는 상한이 걸려 있다(`WRITE_LIMIT`). 새 공개 쓰기 길을 만들면 여기에도 건다.
- 보안 헤더는 `SEC_HEADERS` 한 곳에서 나간다.

## 하지 않는 것

- **배포하지 않는다.** `fly deploy` 는 사람이 직접 한다. 에이전트는 PR 까지다.
- 키·토큰·비밀번호를 코드에 적지 않는다. 환경변수로 뺀다.
- 생성된 파일을 직접 고치지 않는다. 그것을 만든 소스를 고친다.
