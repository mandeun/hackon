# 어떻게 키울 것인가

라켓온과 같은 순서를 쓴다. 시나리오 → 화면 → 서버 → 검사. 다만 이 서비스는
**사람이 안 모이면 아무 기능도 의미가 없다.** 그래서 성장 순서를 먼저 적는다.

---

## 0. 한 줄

> 회사는 **결과**를 사고 싶어 하고, 개인은 **대회를 열 방법**이 없다.
> 둘을 같은 곳에 두고, 끝난 뒤 2주를 같이 판다.

---

## 1. 왜 개인이 열 수 있어야 하나

대행만 하면 매출은 나와도 **재고가 안 쌓인다.** 한 건 끝나면 다시 영업이다.
개인·동아리·소규모 팀이 아무 때나 대회를 열게 두면 세 가지가 생긴다.

| 생기는 것 | 왜 중요한가 |
|---|---|
| 열린 대회 목록 | 협찬사에게 보여 줄 "이미 돌아가는 곳" |
| 참가자 명단 | 대행 수주할 때 "사람 모을 수 있다"의 증거 |
| 성과 기록 | 다음 해 제안서의 근거 (면접·도입검토 건수) |

그래서 **대회 개설을 5분으로 줄이는 것**이 1번 기능이다.
이름·날짜만 넣으면 되고 심사 기준은 기본값이 들어간다.

---

## 2. 처음 100일

### 1~30일 — 우리가 직접 연다
- 대회 3개를 우리 이름으로 연다. 규모는 20~50명. 상금은 없거나 30만 원.
- 목표는 참가자 수가 아니라 **완주율**과 **후기 3줄**.
- 협찬은 현금 대신 **크레딧**으로 받는다. AI 회사들은 자기 도구를 쓰게 하려고
  사용권을 잘 내준다. 현금 0원으로 시작할 수 있는 유일한 길이다.

### 31~60일 — 남이 열게 한다
- 대학 동아리·사내 스터디에 "여기서 열어라" 하고 링크를 준다. 무료.
- 이때 필요한 건 기능이 아니라 **운영 매뉴얼**이다. 8주 준비 목록, 심사표,
  사고 대응 시나리오를 그대로 공개한다. 베껴 가도 좋다 — 실행은 못 베낀다.
- 목표: 우리가 안 낀 대회 5개.

### 61~100일 — 회사에 판다
- 앞의 8개 대회에서 나온 숫자로 제안서를 쓴다.
  참가 n명 · 완주율 n% · 면접 연결 n건 · 도입 검토 n건.
- 첫 유료 건은 원가로 받는다. 목표는 돈이 아니라 **로고와 추천사**다.

---

## 3. 협찬을 받는 순서

협찬 담당자가 결재를 올리려면 **숫자 세 개**가 필요하다. 그래서 제안서에 이것부터 적는다.

1. **완주율** — 끝까지 결과물을 낸 팀의 비율. 돈이 헛돌지 않았다는 증거.
2. **면접 연결 건수** — 협찬사가 실제로 만나 본 사람 수.
3. **도입 검토 건수** — 협찬사가 "써 볼까" 하고 회의에 올린 결과물 수.

이 셋은 `outcomes` 표에 쌓인다. 행사 뒤 2주가 이 표를 채우는 기간이다.

### 협찬 등급 (기본값)

| 등급 | 받는 것 | 주는 것 |
|---|---|---|
| 크레딧 | 도구 사용권 | 로고 + 참가자에게 도구 안내 |
| 상품 | 물품·기프티콘 | 로고 + 부문상 이름 |
| 현금 소액 | 100만 원 이하 | 위 + 과제 1개 출제 |
| 현금 | 500만 원 이상 | 위 + 심사위원 참여 + 성과 보고서 + 면접 연결 |

**현금 등급부터 성과 보고서가 붙는다.** 그게 다음 해 재계약의 이유가 된다.

---

## 4. 라켓온에서 가져온 것

새로 짠 것은 도메인(대회·팀·심사·협찬)뿐이다. 나머지는 그대로 옮겼다.

| 가져온 것 | 라켓온에서 | 여기서 |
|---|---|---|
| `reuse:db-open` | 코트·판 저장 | 대회·팀·점수 저장 |
| `reuse:http-kit` | 그대로 | 그대로 |
| `reuse:router` | `/api/sessions` | `/api/events` |
| `reuse:static` | 그대로 | 그대로 |
| 두 갈래 화면 | `file://` 데모 · `http://` 서버 | 같음 |
| 자체 점검 | `--test` | 같음 (8가지) |

의존성은 라켓온보다 더 줄였다. **0개.** DB도 HTTP도 Node 안에 있다.

---

## 5. 다음에 할 것

- [x] 심사위원 화면 — 팀을 눌러 항목별 점수를 넣는 화면
- [x] 제출 화면 — 참가팀이 링크·설명을 올리는 화면
- [x] 대회 페이지 공개 링크 — `/e/<대회id>` 로그인 없이 보이는 읽기 전용 화면
- [x] 운영 매뉴얼 문서 ([GUIDE.md](GUIDE.md)) — 8주 준비표·장소·인터넷·식사·심사·세금·보험·사고 대응
- [x] 라켓온의 `check-e2e.py` 방식 완주 테스트 이식 — 빈 DB·임시 포트로 11단계

- [x] 로고 — 콜론에서 뽑았다. 꺼진 점과 켜진 점
- [x] 협찬사 등록 화면 — 서버에만 있고 화면이 없어서 공개 페이지가 비어 있던 자리
- [x] 명단 내려받기 · 주소 복사
- [x] 대회 상태 · 심사 진행 현황
- [x] 윈도우 프로그램 — `--app` 창. 창을 닫으면 서버도 꺼진다

§5 가 다 찼다. 다음은 기능이 아니라 **사람**이다 — 1~30일 표대로 대회 3개를 직접 연다.

---

## 6. 다음 네 가지 — 만드는 순서와 이유

기능을 고르는 기준은 하나다. **11월 7일 그날 없으면 곤란한 것부터.**
그리고 마지막 하나는 이 서비스가 실제로 파는 물건이다.

| 순서 | 무엇 | 왜 이 자리인가 |
|---|---|---|
| 1 | 제출 마감 시각과 남은 시간 | 그날 없으면 바로 곤란하다. 스키마 한 칸이면 된다 |
| 2 | 참가 신청 폼 | 9월 26일 모집 시작 전에 있어야 한다. 3·4번이 이 데이터를 쓴다 |
| 3 | 사후 지원 배정 | **행사 전에** 사람을 정해 둬야 한다. 끝나고 정하면 아무도 안 한다 |
| 4 | 결과 보고서 | 앞의 셋이 다 있어야 채울 게 생긴다. 그래서 마지막 |

> 원래 보고서를 3번에 두려 했는데 뒤로 미뤘다.
> 신청 폼과 사후 지원이 없으면 보고서에 빈칸이 생긴다. 빈칸 있는 보고서는 안 보내느니만 못하다.

### 6-1. 제출 마감 시각  ✔ 끝

- 서버: `events` 에 `due TEXT` 한 칸 (`YYYY-MM-DDTHH:MM`). 없으면 지금처럼 동작한다
- 화면: 순위·팀·공개 화면 위에 남은 시간. 1시간 미만이면 색이 바뀐다
- 마감이 지나면 제출 칸이 잠긴다. **서버가 막는다** — 화면만 막으면 안 막은 것이다
- 진행자에게 30분 연장 권한을 준다 (매뉴얼 §3 에 적어 둔 대비책)
- 검사: 마감 전 제출 200, 마감 후 제출 409, 연장 뒤 다시 200

### 6-2. 참가 신청 폼  ✔ 끝

지금은 팀 이름과 연락처뿐이다. 모집 글에 붙일 수 있는 폼이 아니다.

- 서버: `teams` 에 `role` `solo` `found` `members` 추가
  - `role` 만들기/기획/디자인 — 팀을 짜 줄 때 쓴다
  - `solo` 혼자 왔는지. 시작할 때 팀 짜기의 근거다
  - `found` 어디서 보셨나 — **2회차 홍보비를 여기다 쓴다**
- 화면: 참가 신청 칸을 늘리고, 공개 링크로 들어온 사람도 신청할 수 있게 한다
- 운영 화면: 유입 경로별 집계 한 줄
- 검사: 신청이 저장되고, 유입 경로 집계가 맞는가

### 6-3. 사후 지원 배정 — 이게 "보장"이다  ✔ 끝

매뉴얼 §11 에 "후속 담당자를 행사 전에 정해 둔다"고 써 놓고 앱은 아무것도 안 했다.
기록만 하는 표(`outcomes`)로는 약속이 안 된다. 약속에는 **사람과 기한**이 있어야 한다.

- 서버: `supporters` 표 (누가 · 소속 · 무엇을 도와줄 수 있나 · 연락처)
  `assignments` 표 (어느 팀을 · 누가 · 언제까지 · 했나)
- 화면: 지원자를 등록하고 팀에 짝지어 준다. 기한은 기본 행사 +14일
- 공개 화면: "이 팀은 OO 님이 O월 O일까지 봐 드립니다" 가 뜬다
  → 참가자에게는 약속이고, 협찬사에게는 증거다
- 지난 기한은 빨갛게 뜬다. 안 지킨 것이 보여야 지킨다
- 검사: 배정이 저장되고, 기한이 지나면 늦음으로 잡히는가

### 6-4. 결과 보고서  ✔ 끝

협찬사에게 보내는 물건. 지금은 사람이 손으로 쓴다.

- `/e/<대회id>/report` 한 장. 인쇄하면 그대로 PDF 가 된다
- 담기는 것: 대회 개요 · 참가와 완주율 · 순위와 제출작 · 협찬사와 등급 ·
  성과 네 숫자 · **사후 지원 이행 현황** · 다음 회차 제안
- 로그인 없이 열린다. 링크 하나가 곧 보고서다
- 검사: 보고서가 열리고, 위 항목이 전부 실제 데이터로 채워지는가

### 네 가지 다 끝났다 (2026-09-04)

남은 것은 코드가 아니라 사람이다. 6-3 이 돌아가려면 지원자 명단이 차 있어야 한다.

### 사람은 따로 구해 둔다

6-3 이 돌아가려면 **행사 전에 지원자 명단이 차 있어야 한다.**
`첫대회/멘토모집.txt` 의 순서를 그대로 쓴다 — 협찬 등급의 멘토 파견이 1순위,
아는 사람 건너 아는 사람이 2순위. 개인에게 대량 발송은 하지 않는다.

---

## 7. 그다음 세 가지

| 순서 | 무엇 | 왜 |
|---|---|---|
| 1 | 심사위원 전용 링크 | **당일 심사가 여기서 막힌다.** 지금은 운영 화면에서만 심사가 된다 |
| 2 | 등록 데스크 — 동의와 체크인 | 개인정보 동의 없이 연락처를 받고 있다. 법이 먼저다 |
| 3 | 심사 편차 | 매뉴얼에 캘리브레이션을 적어 놓고 앱은 안 도왔다 |

### 7-1. 심사위원 전용 링크  ✔ 끝

`/j/<대회id>` — 로그인 없이 열리고 **점수만** 넣는다.

- 안 보이는 것: 협찬사 · 성과 · 참가자 연락처 · 명단 내려받기 · 마감 연장
- **다른 팀 점수와 순위도 안 보인다.** 심사 중에 순위를 보면 점수가 끌려간다
- 보이는 것: 팀 이름 · 제출 링크와 설명 · 심사 기준 · **내가 낸 점수**
- 아직 안 본 팀이 위에 온다. "누가 아직 안 냈어" 를 심사위원 스스로 알게 한다
- 검사: 심사 링크에서 점수가 저장되고, 남의 점수와 순위가 안 새는가

### 7-2. 등록 데스크 — 동의와 체크인  ✔ 끝

매뉴얼 10장에 "등록 시 수집·이용 동의서를 받는다" 고 적어 놓고 앱은 그냥 연락처를 받았다.

- 신청할 때 동의를 받는다: 개인정보 수집·이용 (필수) · 촬영과 사진 공개 (선택)
- 항목·목적·보유기간을 화면에 적는다. 동의 안 하면 신청이 안 된다
- 동의한 시각을 남긴다. 나중에 "받았다" 를 증명할 것은 그것뿐이다
- 당일 체크인 — 온 팀을 눌러 표시한다. 안 온 팀이 바로 보인다
- 검사: 동의 없이 신청하면 막히는가, 체크인이 남는가

### 7-3. 심사 편차  ✔ 끝

매뉴얼 7장: 심사 전에 한 팀을 같이 채점해 눈높이를 맞춘다(캘리브레이션).

- 심사위원별 평균을 보여 준다. 누가 후하고 누가 짠지 한 줄로
- 가장 후한 사람과 가장 짠 사람의 차이가 크면 경고한다
- **운영자만 본다.** 심사위원에게 보이면 서로 눈치를 본다
- 검사: 편차가 계산되고, 심사 링크에는 안 새는가

### 7장도 다 끝났다 (2026-09-04)

11월 7일을 굴리는 데 앱에서 부족한 것은 이제 없다.
남은 것은 사람이다 — 지원자 명단과 협찬 메일.
